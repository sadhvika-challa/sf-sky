import contract from '../../config/public-url-contract.json'

export const PUBLIC_APP_PATH = contract.paths.app
export const PUBLIC_PRIVACY_PATH = contract.paths.privacy
export const PUBLIC_SUPPORT_PATH = contract.paths.support
export const PUBLIC_MANIFEST_PATH = contract.paths.manifest
export const PUBLIC_SERVICE_WORKER_PATH = contract.paths.serviceWorker
export const PUBLIC_THIRD_PARTY_NOTICES_PATH = contract.paths.thirdPartyNotices

export const PUBLIC_PWA_IDENTITY = contract.pwa
export const PUBLIC_PWA_ICONS = contract.icons
